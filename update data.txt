CREATE TABLE cricket_stats (
    p_match INTEGER,
    inns INTEGER,
    bat VARCHAR(50),
    p_bat INTEGER,
    team_bat VARCHAR(50),
    bowl VARCHAR(50),
    p_bowl INTEGER,
    team_bowl VARCHAR(50),
    ball INTEGER,
    ball_id FLOAT,
    outcome VARCHAR(50),
    score INTEGER,
    out BOOLEAN,
    dismissal VARCHAR(50),
    p_out INTEGER,
    over INTEGER,
    noball INTEGER,
    wide INTEGER,
    byes INTEGER,
    legbyes INTEGER,
    cur_bat_runs INTEGER,
    cur_bat_bf INTEGER,
    cur_bowl_ovr FLOAT,
    cur_bowl_wkts INTEGER,
    cur_bowl_runs INTEGER,
    inns_runs INTEGER,
    inns_wkts INTEGER,
    inns_balls INTEGER,
    inns_runs_rem INTEGER,
    inns_balls_rem INTEGER,
    inns_rr FLOAT,
    inns_rrr FLOAT,
    target INTEGER,
    max_balls INTEGER,
    date DATE,
    year INTEGER,
    ground VARCHAR(50),
    country VARCHAR(50),
    winner VARCHAR(50),
    toss VARCHAR(50),
    competition VARCHAR(50),
    bat_hand VARCHAR(50),
    bowl_style VARCHAR(50),
    bowl_kind VARCHAR(50),
    batruns INTEGER,
    ballfaced INTEGER,
    bowlruns INTEGER,
    bat_out BOOLEAN,
    wagonX FLOAT,
    wagonY FLOAT,
    wagonZone INTEGER,
    line VARCHAR(50),
    length VARCHAR(50),
    shot VARCHAR(50),
    control INTEGER,
    predscore INTEGER,
    wprob FLOAT
);
